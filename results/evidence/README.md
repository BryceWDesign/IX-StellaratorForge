# Evidence

`IXFUSION-POC-001.json` binds the release POC to configuration hashes, assumptions,
limitations, solver-status observations, and machine outputs.

`loss_ledger.json` records which important loss mechanisms are screened and which remain
`UNKNOWN`. Unknown entries are intentional claim-control artifacts, not missing documentation.
