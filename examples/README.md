# Examples

The release results are themselves the primary examples because they are executable,
versioned, and checked by the quality gate.

Run:

```bash
python scripts/run_poc.py
python scripts/run_secondary_studies.py
```

Then inspect `results/poc/POC_RESULT.md` and the JSON evidence under `results/`.
